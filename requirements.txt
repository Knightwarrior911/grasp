mcp>=1.2.0
mss>=9.0.0
pyautogui>=0.9.54
pillow>=10.0.0
pyperclip>=1.8.0
pygetwindow>=0.0.9
# optional, enable extra capabilities:
pydirectinput-rgx>=2.1.0   # SendInput/scancode backend for games & RDP (direct=True)
pytesseract>=0.3.10        # find_text OCR (also needs a Tesseract-OCR install on PATH)
uiautomation>=2.0.18       # locate()/snapshot tier 1: Windows accessibility tree (exact, free)
rapidocr-onnxruntime>=1.3  # locate()/read_screen tier 2: on-screen OCR (offline, free; no Tesseract)
psutil>=5.9                # system control: list/kill processes, process_running
pyttsx3>=2.90              # speak(): offline TTS (hands-free / voice-Grasp loop)
pynput>=1.7                # macro_record_start/stop: capture real mouse + keyboard
pycaw>=20230407            # volume(): system audio endpoint (needs comtypes)
comtypes>=1.2              # pycaw COM dependency
screen-brightness-control>=0.16  # brightness()
